# Changelog

## 0.1.0 — first cut

- `add`, `show`, `list`, `stats`, `rules`, `import`, `init`.
- Word-level diff with paragraph alignment, so a rewrapped send is not an edit.
- Rule mining: killed phrases, added phrases, length, openers, closers,
  structure. Function-word filter, baseline lift and fragment chaining on top of
  the occurrence and consistency bars.
- Trend: last five pairs against the five before.
- Twelve worked example pairs and the real `rules` output over them.
